HTTP SERVER: server.cpp

Compile using: g++ server.cpp -o<exe_name>

Usage: <exe_name> <config_file>

The server will read in the config file and open a listening port, it will then wait for 
connections on that port and respone do HTTP requests.
